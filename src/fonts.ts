/**
 * Font configuration for Remotion.
 *
 * In production with network access, replace this with:
 *   import { loadFont } from '@remotion/google-fonts/Inter';
 *   const { fontFamily } = loadFont('normal', { weights: ['300','400','500','600','700'], subsets: ['latin'] });
 *
 * For offline/CI environments or to guarantee availability, place Inter
 * .woff2 files in public/ and use @remotion/fonts loadFont() with staticFile().
 */

export const fontFamily =
  'Inter, -apple-system, BlinkMacSystemFont, Segoe UI, Helvetica Neue, Arial, sans-serif';
